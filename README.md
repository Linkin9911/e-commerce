# E‑commerce Core

Ядро интернет‑магазина на Python с использованием ООП. Проект реализует основные сущности и бизнес‑логику для управления товарами и категориями.

## Функционал

* Класс `Product`:
  * приватный атрибут цены с геттером и сеттером;
  * проверка корректности цены (не может быть нулевой или отрицательной);
  * класс‑метод `new_product()` для создания продукта из словаря.
* Класс `Category`:
  * приватный список товаров;
  * метод `add_product()` для добавления товаров;
  * геттер `products` для форматированного вывода списка товаров.
* Автоматическое отслеживание количества категорий и товаров.
* Загрузка данных из JSON.

## Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <URL-репозитория>
   cd <название-проекта>
Установите зависимости через Poetry:

bash
poetry install
Использование
Пример создания товаров и категорий:

python
# Создание товаров
product1 = Product("Смартфон", "Современный смартфон", 29999.99, 10)
product2 = Product("Ноутбук", "Мощный ноутбук", 59999.99, 5)

# Создание категории и добавление товаров
category = Category("Электроника", "Электронные устройства")
category.add_product(product1)
category.add_product(product2)

# Вывод списка товаров в категории
print(category.products)
Создание товара через класс‑метод:

python
product_data = {
    "name": "Планшет",
    "description": "10-дюймовый планшет",
    "price": 35000.0,
    "quantity": 7
}
new_product = Product.new_product(product_data)
Тестирование
Запустите тесты с отчётом о покрытии:

bash
poetry run pytest --cov=src --cov-report=term-missing --cov-report=html:htmlcov
Покрытие кода тестами — более 75 %. Отчёт доступен в папке htmlcov/index.html.

Критерии приёмки
Реализация соответствует всем критериям задания:

приватные атрибуты для защиты данных;

геттеры и сеттеры для контролируемого доступа;

класс‑методы для альтернативного создания объектов;

обработка крайних случаев (отрицательные цены);

полное покрытие функциональности тестами (> 75 %).